export type LayoutItemsType = {
  id: number;
  title: string;
  description: string;
  image: string;
  tags: string[];
  category: string;
  price: number;
  rating: number;
  inStock: boolean;
  createdAt: string; // ISO date string
};
